apiVersion: release-notes/v2
kind: feature
area: installation
releaseNotes:
  - |
    **Added** an `-o/--output` flag to `istioctl manifest generate` that writes
    the generated manifest to a file instead of stdout. This is useful in
    environments without a shell to redirect output, such as the distroless
    `istioctl` image.
