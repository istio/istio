apiVersion: release-notes/v2
kind: feature
area: istioctl
issue:
  - 43807
releaseNotes:
  - |
    **Added** the config type for each configuration summary when using `istioctl pc all`.
  - |
    **Added** the endpoint configuration summary to `istioctl pc all`.
