templates:
  sidecar: |-
    spec:
      initContainers:
      - name: istio-init
        image: registry.istio.io/release/proxy_init:unittest-{{.Values.global.tag}}
      containers:
      - name: istio-proxy
        image: registry.istio.io/release/proxy_debug:unittest
