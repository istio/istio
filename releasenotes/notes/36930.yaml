apiVersion: release-notes/v2
kind: feature
area: installation

releaseNotes:
  - |
    **Added** customizable image to istio operator manifests, adding `--image` to both `istioctl operator init` and `istioctl operator dump`.
