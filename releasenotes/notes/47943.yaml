apiVersion: release-notes/v2
kind: feature
area: installation
releaseNotes:
- |
  **Removed** an issue where the default namespace of envoy and proxy dashboard command was not set to the actual default namespace.
