{{- if .Values.global.remotePilotCreateSvcEndpoint }}
apiVersion: v1
kind: Service
metadata:
  name: istio-pilot
  namespace: {{ .Release.Namespace }}
spec:
  ports:
  - port: 15010
    name: grpc-xds # direct
  - port: 15011
    name: secured-grpc-xds # mTLS
  - port: 9093
    name: http-monitoring
  clusterIP: None
{{- end }}
