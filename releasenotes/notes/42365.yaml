apiVersion: release-notes/v2
kind: feature
area: istioctl

releaseNotes:
- |
  **Added** support for retrieving typed extension configuration from Envoy in the specified pod by running `istioctl proxy-config ecds`.
