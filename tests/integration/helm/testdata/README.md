# Upgrade testdata

This folder contains a snapshot of older released versions of Istio helm charts to test upgrades

## Adding versions

To add a version, download the release and copy the base, istio-control, and gateway charts, tar.gz compressed.
