apiVersion: v1
kind: Service
metadata:
  name: statsdadapter
  labels:
    app: statsdadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: statsdadapter
