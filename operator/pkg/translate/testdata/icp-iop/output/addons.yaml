apiVersion: operator.istio.io/v1alpha1
kind: IstioOperator
spec:
  addonComponents:
    grafana:
      enabled: true
    kiali:
      enabled: true
    tracing:
      enabled: true
  values:
    grafana:
      enabled: true
    kiali:
      createDemoSecret: true
      enabled: true
    tracing:
      enabled: true
