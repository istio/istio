apiVersion: v1
kind: Service
metadata:
  name: memquotaadapter
  labels:
    app: memquotaadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: memquotaadapter
