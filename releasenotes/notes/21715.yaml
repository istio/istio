apiVersion: release-notes/v2
kind: feature
area: installation
issue:
- 21715
releaseNotes:
- |
  **Added** post-install verification of control plane health. Add `--verify` to `istioctl install` command.