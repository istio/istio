apiVersion: release-notes/v2
kind: bug-fix
area: istioctl
releaseNotes:
  - |
    **Optimized** Replace `IST0104 - GatewayPortNotOnWorkload` to new message `IST0162 - GatewayPortNotDefinedOnService` alert in 
    the `IngressGatewayPortAnalyzer`, and retired `IST0104`.
