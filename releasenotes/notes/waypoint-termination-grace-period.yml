apiVersion: release-notes/v2
kind: feature
area: traffic-management
releaseNotes:
- |
  **Added** a new `terminationGracePeriodSeconds` field to the waypoint template to allow for a conditional termination grace period.
