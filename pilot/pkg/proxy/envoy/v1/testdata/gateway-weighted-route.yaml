hosts:
- world.example.com
gateways:
- some-gateway
http:
- route:
  - destination:
      host: world
      subset: v0
    weight: 75
  - destination:
      host: world
      subset: v1
    weight: 25
