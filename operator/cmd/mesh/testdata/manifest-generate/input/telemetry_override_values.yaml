apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  profile: empty
  components:
    telemetry:
      enabled: true
  values:
    mixer:
      adapters:
        prometheus:
          metricsExpiryDuration: 123m


