apiVersion: release-notes/v2
kind: feature
area: traffic-management
issue:
- 48876
releaseNotes:
- |
  **Added** A new option for `install-cni`, `--ambient-reset-previous-connections`, has been created for reset previous
  connections after inject redirection rules to pod network namespace.
