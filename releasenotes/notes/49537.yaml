apiVersion: release-notes/v2
kind: bug-fix
area: traffic-management
issue:
  - 49537
releaseNotes:
  - |
    **Fixed** an issue that when using `withoutHeaders` matching,
    if the header is missing, it should be treated as empty value for matching.
