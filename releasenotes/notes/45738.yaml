apiVersion: release-notes/v2

kind: feature

area:
  - installation

issue: 45738

releaseNotes: 
- |
  Adding the option of hostNetwork to the istiod helm chart to allow the option for users to use the host network for pilot
