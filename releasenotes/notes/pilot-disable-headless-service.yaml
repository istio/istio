apiVersion: release-notes/v2
kind: feature
area: traffic-management
issue:
  - 60552
releaseNotes:
  - |
    **Added** environment variable `PILOT_ENABLE_HEADLESS_SERVICE` (default `true`) to allow users to disable
    special processing for Kubernetes headless services.
