apiVersion: release-notes/v2
kind: feature
area: istioctl
releaseNotes:
- |
  **Added** support specifying proxy admin port for `istioctl experimental authz`, `istioctl proxystatus`, `istioctl bug-report` and `istioctl experimental describe`.
