apiVersion: release-notes/v2
kind: feature
area: istioctl
issue: [56005]
releaseNotes: 
- |
  **Changed** `istioctl proxy-status` to dynamically display all xDS/CRD types as columns in the output table.
