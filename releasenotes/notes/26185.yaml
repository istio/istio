apiVersion: release-notes/v2
kind: feature

area: security

releaseNotes: |
  Fix the multi cluster service discovery client SAN generation: taking endpoints' service accounts as the list.
  Istiod generates the client SAN verification spiffie identity with cluster's own trust domain into consideration.
  This makes cluster with different trust domain able to communicate via mTLS.
