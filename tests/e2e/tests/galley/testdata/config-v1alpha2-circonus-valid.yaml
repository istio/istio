apiVersion: "config.istio.io/v1alpha2"
kind: handler
metadata:
  name: valid-circonus
spec:
  compiledAdapter: circonus
  params:
    submission_url: "https://trap.noit.circonus.net/module/httptrap/myuuid/mysecret"
    submission_interval: "10s"
