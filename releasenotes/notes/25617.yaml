apiVersion: release-notes/v2
kind: bug-fix
area: istioctl
issue: 23868

# No releaseNotes
# Although this item has a user-facing change (--authority instead of --xds-san), the
# internal name --xds-san was not intended to be exposed.  This flag was not in 1.6
# and its documentation will be generated using Cobra.
