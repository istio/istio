apiVersion: release-notes/v2
kind: feature
area: traffic-management
releaseNotes:
- |
  **Added** `ENABLE_EXTERNAL_NAME_SERVICE` feature flag, which defaults to true conserving the current behavior processing the ExternalName service. It can be set to false removing the ExternalName service's influence completely.
