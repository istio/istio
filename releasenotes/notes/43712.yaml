apiVersion: release-notes/v2
kind: bug-fix
area: istioctl
issue:
  - 40861
releaseNotes:
  - |
    **Added** a bug in `istioctl analyze` where unexpected messages are displayed when analyzing files that contain gateway resources.
