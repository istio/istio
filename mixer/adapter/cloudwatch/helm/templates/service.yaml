apiVersion: v1
kind: Service
metadata:
  name: cloudwatchadapter
  labels:
    app: cloudwatchadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: cloudwatchadapter
