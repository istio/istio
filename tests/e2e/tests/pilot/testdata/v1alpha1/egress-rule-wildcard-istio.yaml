apiVersion: config.istio.io/v1alpha2
kind: EgressRule
metadata:
  name: fortio-wildcard
spec:
  destination:
      service: "*.istio.io"
  ports:
      - port: 80
        protocol: http
  use_egress_proxy: false
