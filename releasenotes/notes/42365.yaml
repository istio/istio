apiVersion: release-notes/v2
kind: feature
area: istioctl

releaseNotes:
- |
  **Added** support for retrieves typed extension configuration for the Envoy in the specified pod by running `istioctl proxy-config ecds`.
