apiVersion: apps/v1
kind: DaemonSet
metadata:
  name: addon-in-test1
  namespace: test1
  labels:
    addon: test2
spec:
  selector:
    matchLabels:
      app: nginx
  template:
    metadata:
      labels:
        app: nginx
        addon: test2
    spec:
      containers:
      - name: hello
        image: "fake.docker.io/google-samples/hello-go-gke:1.0"
        ports:
          - name: http
            containerPort: 80
      tolerations:
      - operator: "Exists"
        effect: "NoSchedule"
