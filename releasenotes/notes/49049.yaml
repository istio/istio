apiVersion: release-notes/v2
kind: feature
area: istioctl
releaseNotes:
- |
  **Added** namespace filtering to `istioctl pc workload` using the `--config-namespace` flag, to display workloads in a specific namespace.
