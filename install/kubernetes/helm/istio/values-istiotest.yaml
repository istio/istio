# Configuration overrides for deploying istio for testing

global:
  nodePort: true
  proxy:
    image: proxy_debug

ingress:
  service:
    nodePort:
      enabled: true


prometheus:
  enabled: true
  ingress:
    enabled: true

grafana:
  enabled: true

servicegraph:
  enabled: true