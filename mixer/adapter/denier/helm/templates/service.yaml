apiVersion: v1
kind: Service
metadata:
  name: denieradapter
  labels:
    app: denieradapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: denieradapter
