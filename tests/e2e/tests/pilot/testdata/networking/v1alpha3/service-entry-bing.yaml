apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: service-entry-bing
spec:
  hosts:
    - www.bing.com
  ports:
  - number: 443
    name: https
    protocol: TLS
  resolution: DNS
