# This is from Bazel's former jenkins setup, to avoid blowing up the RAM usage.
startup --host_jvm_args=-Xmx4000m
startup --host_jvm_args=-Xms4000m
startup --batch

# This is so we understand failures better
build --verbose_failures

