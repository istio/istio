apiVersion: apiextensions.k8s.io/v1beta1
kind: CustomResourceDefinition
metadata:
  name: producerservices.config.istio.io
spec:
  group: config.istio.io
  names:
    kind: ProducerService
    listKind: ProducerServiceList
    plural: producerservices
    singular: producerservice
  scope: Namespaced
  version: dev

