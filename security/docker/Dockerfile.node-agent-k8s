# TODO(incfly): change to smaller base image before release.
FROM ubuntu:16.04
ADD node_agent_k8s /
RUN mkdir -p /etc/istio
ADD nodeagent-sds-cert.pem /etc/istio
ADD nodeagent-sds-key.pem /etc/istio
CMD ["/node_agent_k8s"]
