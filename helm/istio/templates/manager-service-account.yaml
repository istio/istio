apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ template "fullname" . }}-manager-service-account
  labels:
    app: {{ template "fullname" . }}-manager-service-account
    chart: "{{ .Chart.Name }}-{{ .Chart.Version | replace "+" "_" }}"
    release: "{{ .Release.Name }}"
    heritage: "{{ .Release.Service }}"
