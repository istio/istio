apiVersion: release-notes/v2
kind: feature
area: telemetry
releaseNotes:
  - |
    **Added** experimental feature send stats configuration with ECDS, guarded by pilot environment flag `ENABLE_ECDS_FOR_STATS`.
