apiVersion: release-notes/v2
kind: feature
area: traffic-management
issue:
  - 29761

releaseNotes:
- |
  **Added** support for start tls configuration for postgres. This feature can be disabled by 
            setting PILOT_ENABLE_POSTGRES to false.
