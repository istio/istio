apiVersion: networking.istio.io/v1alpha3
kind: ServiceEntry
metadata:
  name: {{ .portName }}-external-se
spec:
  hosts:
  - {{ .externalHost }}
  location: MESH_EXTERNAL
  ports:
  - number: {{ .portNumber }}
    name: {{ .portName }}
    protocol: {{ .portProtocol }}
  resolution: DNS