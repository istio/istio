destination: ratings.default.svc.cluster.local
precedence: 2
match:
  http:
    Cookie:
      regex: "^(.*?;)?(user=jason)(;.*)?$"
route:
- tags:
    version: v1
http_fault:
  delay:
    percent: 100
    fixed_delay_seconds: 7.0
