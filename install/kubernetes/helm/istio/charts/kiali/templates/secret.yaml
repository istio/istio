apiVersion: v1
kind: Secret
metadata:
  name: {{ if .Values.dashboard.secretName }}{{ .Values.dashboard.secretName }}{{ else }}{{ template "kiali.fullname" . }}{{ end }}
  labels:
    app: {{ template "kiali.name" . }}
    chart: {{ template "kiali.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
type: Opaque
data:
  username: {{ .Values.dashboard.username | b64enc | quote }}
  {{- if .Values.dashboard.passphrase }}
  passphrase: {{ .Values.dashboard.passphrase | b64enc | quote }}
  {{- else }}
  passphrase: {{ randAlphaNum 40 | b64enc | quote }}
  {{- end }}
