apiVersion: release-notes/v2
kind: bug-fix
area: installation

issue:
- 42055

releaseNotes:
- |
  **Updated** the default directory that OpenShift is installed to from `kube-system` to `openshift-operators`. Please see the OCP platform documentation for further information.

upgradeNotes:
- title: istio-cni chart needs to be installed to a different namespace in OCP 4.12
  content: istio-cni needs to be removed from kube-system prior to installation and upgrade.

docs:
- https://istio.io/latest/docs/setup/platform-setup/openshift/
