apiVersion: release-notes/v2
kind: feature
area: telemetry
releaseNotes:
  - |
    **Added** metric expiry support, when `ISTIO_META_METRIC_ROTATION_INTERVAL` and
    `ISTIO_META_METRIC_GRACEFUL_DELETION_INTERVAL` are enabled in `ProxyConfig.proxyMetadata`.
