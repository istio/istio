apiVersion: release-notes/v2
kind: feature
area: installation
issue:
  - 41057
  - 43585
releaseNotes:
  - |
    **Added** support for allowing labels to be added to the gateway pod template via `.Values.labels`.
