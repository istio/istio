# Default ASM Profile for GCP Projects.
#   * Security: Mesh CA
#   * Telemetry: Stackdriver
#   * Networking: istiod
apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  meshConfig:
    defaultConfig:
      proxyMetadata:
        CA_PROVIDER: "GoogleCA"
        PLUGINS: "GoogleTokenExchange"
    # Protocol detection timeout is not supported
    protocolDetectionTimeout: 0s
    # Locality load balancing is not supported
    localityLbSetting:
      enabled: false
  components:
    # Egress is supported but not default
    egressGateways:
    - enabled: false
      name: istio-egressgateway
    # The default profile is tuned to fit on smaller clusters.
    # Increase cpu req to ensure we can handle larger scale clusters.
    pilot:
      k8s:
        resources:
          requests:
            cpu: 1000m
        hpaSpec:
          minReplicas: 2
        replicaCount: 2
        env:
        # Env var used for control plane Stackdriver monitoring enablement.
        - name: ENABLE_STACKDRIVER_MONITORING
          value: true
        # POD_NAME and POD_NAMESPACE are used for control plane metrics monitored resource.
        - name: ASM_CONTROL_PLANE_POD_NAME
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.name
        - name: ASM_CONTROL_PLANE_POD_NAMESPACE
          valueFrom:
            fieldRef:
              apiVersion: v1
              fieldPath: metadata.namespace
  addonComponents:
    # Grafana is disabled by default, but supported with --set values.grafana.enabled=true
    # This requires enabling prometheus.
    grafana:
      enabled: false
    # Prometheus is disabled by default
    prometheus:
      enabled: false
  values:
    # Protocol sniffing is not supported
    pilot:
      enableProtocolSniffingForOutbound: false
      enableProtocolSniffingForInbound: false
    # Enable telemetry v2 backend by Stackdriver.
    # Prometheus is also supported with --set values.telemetry.v2.prometheus.enabled=true --set prometheus.enabled=true
    telemetry:
      enabled: true
      v1:
        enabled: false
      v2:
        enabled: true
        prometheus:
          enabled: false
        stackdriver:
          enabled: true
          logging: true
          topology: true
    sidecarInjectorWebhook:
      rewriteAppHTTPProbe: true
    global:
      caAddress: "meshca.googleapis.com:443"
      pilotCertProvider: kubernetes
      # Enable permissive mTLS by default
      mtls:
        enabled: false
      sts:
        servicePort: 15463
