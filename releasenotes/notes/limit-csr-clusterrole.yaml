apiVersion: release-notes/v2
kind: feature
area: installation
releaseNotes:
- |
  **Added** check to limit the clusterrole for k8s CSR permissions for custom CA usecases by verifying EXTERNAL_CA and defaultCertProvider parameters.
