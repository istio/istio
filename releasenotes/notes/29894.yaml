apiVersion: release-notes/v2
kind: feature
area: istioctl
issue:
  - 29026

releaseNotes:
- |
  **Added** `WorkloadEntry` resources will be read from all clusters in multi-cluster installations and do not need to be duplicated.
  Virtual Machine auto-registration is now compatible with multi-primary multi-cluster.
