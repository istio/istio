apiVersion: v1
kind: Service
metadata:
  name: signalfxadapter
  labels:
    app: signalfxadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: signalfxadapter
