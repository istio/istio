apiVersion: release-notes/v2
kind: feature
area: networking
releaseNotes:
- |
  **Updated** `ProxyConfig` resources with workload selector will be applied to Kubernetes `Gateway` objects
  only if the label is `istio.io/gateway-name`. Other labels are ignored.
