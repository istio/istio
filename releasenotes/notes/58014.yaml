apiVersion: release-notes/v2
kind: bug-fix
area: traffic-management
issue: [58014]
releaseNotes:
- |
  **Fixed** race condition in KRT index collection causing intermittent HTTP 500 errors during simultaneous HTTPRoute updates.
