apiVersion: release-notes/v2
kind: feature
area: networking
issue:
  - 60121
releaseNotes:
  - |
    **Added** support for on-demand CDS(Cluster Discovery Service) with pilot environment variable `PILOT_ENABLE_LAZY_CDS`.
