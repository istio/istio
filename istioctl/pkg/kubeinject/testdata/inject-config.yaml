templates:
  sidecar: |-
    spec:
      initContainers:
      - name: istio-init
        image: registry.istio.io/release/proxy_init:unittest-{{.Values.global.suffix}}
      containers:
      - name: istio-proxy
        image: registry.istio.io/release/proxy_debug:unittest
