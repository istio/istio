apiVersion: v1
kind: Service
metadata:
  name: fluentdadapter
  labels:
    app: fluentdadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: fluentdadapter
