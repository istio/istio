apiVersion: config.istio.io/v1alpha2
kind: V1alpha2RouteRule
metadata:
  name: route-rule-c
spec:
  hosts:
    - c
  http:
    - route:
      - destination:
          name: c
          subset: v1
        weight: 100
      corsPolicy:
        allowOrigin:
          - http://foo.example
        allowMethods:
          - POST
          - GET
          - OPTIONS
        allowHeaders:
          - content-type
        exposeHeaders:
          - x-custom-header
        maxAge: 300s
        allowCredentials: true
