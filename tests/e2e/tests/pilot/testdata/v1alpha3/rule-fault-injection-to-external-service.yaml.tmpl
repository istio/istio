apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: fault-injection-to-external-service-rule
spec:
  hosts:
  - "{{.host}}"
  http:
  - route:
    - destination:
        host: "{{.host}}"
    fault:
      abort:
        percent: 100
        httpStatus: 418
