apiVersion: config.istio.io/dev
kind: ProducerService
metadata:
  name: bar
spec:
  serviceSelector:
    name: bar
  rules:
    - match: bar == 42
      actions:
        - handler: stackdriver
          instances:
            - template: quota
              params:
                foo: bar

  handlers:
    - name: stackdriver