apiVersion: security.istio.io/v1
kind: AuthorizationPolicy
metadata:
  name: invalid-authorization-policy
spec:
  selector:
    matchLabels:
      app: httpbin
      version: v1
  rules:
  - from:
    - source:
        ipBlocks: ["not-ip"]
    to:
    - operation:
        methods: ["GET"]
