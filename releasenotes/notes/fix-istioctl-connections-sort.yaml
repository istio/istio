apiVersion: release-notes/v2
kind: bug-fix
area: istioctl
issue:
  - https://github.com/istio/istio/pull/59775
releaseNotes:
  - |
    **Fixed** a sorting bug in `istioctl ztunnel-config` connections which caused the output sorting to be non-deterministic.
