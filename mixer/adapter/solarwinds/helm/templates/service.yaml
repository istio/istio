apiVersion: v1
kind: Service
metadata:
  name: solarwindsadapter
  labels:
    app: solarwindsadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: solarwindsadapter
