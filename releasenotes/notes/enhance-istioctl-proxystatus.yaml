apiVersion: release-notes/v2
kind: feature
area: istioctl
issue:
  - 31474
releaseNotes:
  - |
    **Improved** `istioctl proxy-status` command to include additional XDS sync statuses
    like NDS, ECDS, PCDS and added options to filter XDS status columns.
