---

apiVersion: clusterregistry.k8s.io/v1alpha1
kind: Cluster
metadata:
  name: cluster-a
  annotations:
    config.istio.io/pilotEndpoint: "1.1.1.1:9080"
    config.istio.io/platform: "k8s"
    config.istio.io/pilotCfgStore: true
    config.istio.io/accessConfigFile: c1-config
spec:
  kubernetesApiEndpoints:
    serverEndpoints:
      - clientCidr: "0.0.0.0/0"
        serverAddress: "192.168.1.1"

---

apiVersion: clusterregistry.k8s.io/v1alpha1
kind: Cluster
metadata:
  name: cluster-b
  annotations:
    config.istio.io/pilotEndpoint: "1.1.1.1:9080"
    config.istio.io/platform: "k8s"
    config.istio.io/accessConfigFile: c2-config
spec:
  kubernetesApiEndpoints:
    serverEndpoints:
      - clientCidr: "0.0.0.0/0"
        serverAddress: "192.168.2.1"

