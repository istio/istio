apiVersion: v1
kind: ConfigMap
metadata:
  name: istio-ztunnel-config
  namespace: {{ .Release.Namespace }}
  labels:
    {{- .Values.labels | toYaml | nindent 4}}
  annotations:
    {{- .Values.annotations | toYaml | nindent 4}}
data:
  mesh: |
    defaultConfig:
      {{- .Values.proxyConfig | toYaml | nindent 6}}
