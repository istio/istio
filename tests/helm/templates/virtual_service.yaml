apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: byon1
spec:
  hosts:
  - byon1
  http:
  - route:
    - destination:
        host: fortio.test.svc.cluster.local
        port:
          number: 8080

---

