apiVersion: install.istio.io/v1alpha1
kind: IstioOperator
spec:
  # install only "base" component
  components:
    pilot:
      enabled: false
    ingressGateways:
      - name: istio-ingressgateway
        enabled: false
