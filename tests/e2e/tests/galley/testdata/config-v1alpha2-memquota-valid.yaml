apiVersion: "config.istio.io/v1alpha2"
kind: handler
metadata:
  name: valid-memquota
spec:
  compiledAdapter: memquota
  params:
    quotas:
    - name: requestcount.quota.istio-system
      maxAmount: 5000
      validDuration: 1s
      overrides:
      - dimensions:
          destination: ratings
          source: reviews
        maxAmount: 1
        validDuration: 1s
