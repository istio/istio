apiVersion: config.istio.io/v1beta1
kind: RouteRule
metadata:
  name: reviews-test-v2
spec:
  destination: reviews
  precedence: 2
  match:
    request:
      headers:
        cookie: /^(.*?;)?(user=jason)(;.*)?$/
  route:
  - labels:
      version: v2
