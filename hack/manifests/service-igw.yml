apiVersion: v1
kind: Service
metadata:
  labels:
    app: internal-ingressgateway-dev
    istio: ingressgateway
  name: internal-ingressgateway-dev
  namespace: istio-system
spec:
  ports:
  - name: http2
    port: 80
    protocol: TCP
    targetPort: 80
  selector:
    app: internal-ingressgateway-dev
    istio: ingressgateway
  sessionAffinity: None
  type: ClusterIP
