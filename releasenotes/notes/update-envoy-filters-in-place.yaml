apiVersion: release-notes/v2
kind: feature
area: traffic-management

releaseNotes:
- |
  Avoid rebuilding all EnvoyFilters everytime a single one has changed, insted only rebuild the changed EnvoyFilter and update it in place.