apiVersion: release-notes/v2
kind: feature
area: installation
issue:
- 21715
releaseNotes:
- |
  **Added** post-install/in-place upgrade verification of control plane health. Use `--verify` flag to `istioctl install` or `istioctl upgrade`