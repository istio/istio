apiVersion: release-notes/v2
kind: feature
area: installation
issue:
  - 43632
releaseNotes:
- |
  **Fixed** Istio CNI helm configurations to use 'cni' consistently instead of having the separate 'istio_cni' configurations to enable Istio CNI when installing using helm charts.
