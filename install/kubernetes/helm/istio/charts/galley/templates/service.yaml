apiVersion: v1
kind: Service
metadata:
  name: istio-galley
  namespace: {{ .Release.Namespace }}
  labels:
    istio: galley
spec:
  type: {{ .Values.service.type }}
  ports:
  - port: 443
  selector:
    istio: galley
