apiVersion: networking.istio.io/v1alpha3
kind: DestinationRule
metadata:
  name: prometheus
  namespace: {{ .Release.Namespace }}
spec:
  host: prometheus
  trafficPolicy:
    tls:
      mode: DISABLE
