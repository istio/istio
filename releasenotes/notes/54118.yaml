apiVersion: release-notes/v2
kind: bug-fix
area: security
issue:
  - 52121
releaseNotes:
- |
  **Fixed** Possible race conditions in JWK resolution cache for JWT policies that potentially caused
  cache misses & failures to successfully cache newly refreshed tokens.
