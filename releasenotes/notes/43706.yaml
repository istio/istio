apiVersion: release-notes/v2
kind: feature
area: istioctl

issue:
- 43705

releaseNotes:
- |
  **Added** `istioctl analyzer` will display a error when encountering the following two situations  
  - If there are multiple Telemetry CRs without selector 
  - If there are multiple Telemetry CRs with selector which selected for the same workload
  
  
