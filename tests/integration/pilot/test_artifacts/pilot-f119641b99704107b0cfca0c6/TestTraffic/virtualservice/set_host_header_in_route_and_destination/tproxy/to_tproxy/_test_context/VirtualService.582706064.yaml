apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: default
  namespace: echo-2-60257
spec:
  hosts:
  - tproxy
  http:
  - headers:
      request:
        set:
          :authority: route-authority
    route:
    - destination:
        host: tproxy
      headers:
        request:
          set:
            Host: dest-authority