apiVersion: v1
kind: Service
metadata:
  name: zipkinadapter
  labels:
    app: zipkinadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: zipkinadapter
