apiVersion: release-notes/v2
kind: bug-fix
area: installation
issue:
  - 57354
releaseNotes:
  - |
    **Fixed** an issue where the Helm 3.18.5 switched to a new JSON schema validator that is more strict and caused installation failures in the Gateway chart.