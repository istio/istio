apiVersion: v1
kind: Service
metadata:
  name: bypassadapter
  labels:
    app: bypassadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: bypassadapter
