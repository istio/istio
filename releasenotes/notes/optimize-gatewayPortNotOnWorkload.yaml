apiVersion: release-notes/v2
kind: bug-fix
area: istioctl
releaseNotes:
  - |
    **Optimized** Replace `GatewayPortNotOnWorkload` to `GatewayPortNotDefinedOnService` alert in 
    the `IngressGatewayPortAnalyzer`, and deprecated it.
