apiVersion: release-notes/v2
kind: feature
area: extensibility
issue: []
releaseNotes:
  - |
    **Added** support for WasmPlugin pulling images from cloud provider registries without setting the
    `imagePullSecret` when running in GKE, AKS and EKS.
