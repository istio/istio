apiVersion: release-notes/v2
kind: feature
area: security
releaseNotes:
- |
  **Added** request.regex.headers condition for authorization policies.  This
  allows matching HTTP headers against a regex.
