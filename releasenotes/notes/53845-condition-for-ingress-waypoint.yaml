apiVersion: release-notes/v2
kind: feature
area: installation
releaseNotes:
  - |
    **Added** a new condition type, "istio.io/IngressUseWaypoint" to represent a service binding to a waypoint proxy for ingress.
