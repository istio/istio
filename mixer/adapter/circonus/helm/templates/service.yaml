apiVersion: v1
kind: Service
metadata:
  name: circonusadapter
  labels:
    app: circonusadapter
spec:
  ports:
  - name: http
    port: {{ .Values.service.portNumber }}
  selector:
    app: circonusadapter
