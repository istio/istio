steps:
  - name: 'gcr.io/kaniko-project/executor:latest'
    args:
      - --destination=gcr.io/$PROJECT_ID/istiod:latest
      - --cache=true
      - --cache-ttl=24h
      - -f docker/Dockerfile_build.istiod
    timeout: 1200s

timeout: 1200s
