apiVersion: release-notes/v2
kind: feature
area: traffic-management
issue:
  - https://github.com/istio/istio/issues/31115
releaseNotes:
  - |
    **Added** Specify `meshConfig.namespaceSelectors` to dynamically restrict the set of namespaces that istiod create istio-ca-root-cert configmap.
