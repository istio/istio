apiVersion: release-notes/v2
kind: feature
area: installation
issue:
  - 39797

releaseNotes:
  - |
    **Added** new istio operator `remote` profile and deprecated the `external` profile, which is equivalent.